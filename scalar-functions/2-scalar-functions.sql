SELECT AVG(salary) FROM employees; -- Calculate the average salary
SELECT COUNT(*) FROM orders; -- Count the number of orders
SELECT MAX(price) FROM products; -- Find the maximum product price
SELECT MIN(quantity) FROM inventory; -- Find the minimum inventory quantity
SELECT SUM(sales) FROM sales; -- Calculate the total sales
